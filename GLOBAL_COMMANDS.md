# 🌍 Глобальні команди

## ✅ Встановлено!

Тепер команди доступні **з будь-якого місця** в терміналі!

### 🎯 Твої глобальні команди:

```bash
power-status      # Вся інформація відразу ⭐
power-plan        # План на день
power-check       # Графіки відключень
power-changes     # Зміни в графіках
power-recommend   # Рекомендація зараз
```

### 🚀 Як активувати:

**Варіант 1 (швидко):**
```bash
source ~/.zshrc
```

**Варіант 2 (просто):**
Відкрий новий термінал - команди вже працюють!

### 📝 Приклад використання:

```bash
# З будь-якої папки:
cd ~/Documents
power-status

# Або
cd /tmp
power-recommend

# Працює звідки завгодно!
```

### 🔧 Що було зроблено:

Скрипт `./install.sh` додав аліаси в `~/.zshrc`:
- Команди автоматично переходять в потрібну папку
- Запускають відповідний скрипт
- Повертаються назад

### 🗑️ Як видалити:

Якщо захочеш видалити команди:

```bash
# Відкрий файл
nano ~/.zshrc

# Видали секцію "# Power Schedule aliases"
# Збережи (Ctrl+O, Enter, Ctrl+X)

# Перезавантаж
source ~/.zshrc
```

### ⚡ Тепер спробуй:

```bash
power-status
```

З будь-якої папки!
