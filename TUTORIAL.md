# 🎓 Навчальний посібник

## Твої команди:

### ⭐ `./status.sh` - ВСЯ ІНФОРМАЦІЯ ВІДРАЗУ

**Найкорисніша команда!** Виконує все в правильному порядку:
1. Перевіряє зміни
2. Показує графіки
3. Складає план
4. Дає рекомендацію

```bash
./status.sh
# АБО з будь-якого місця:
power-status
```

**Використовуй це щоранку або коли хочеш швидко оцінити ситуацію!**

> 💡 **Підказка:** Після запуску `./install.sh` команди доступні глобально!
> Детальніше: [GLOBAL_COMMANDS.md](GLOBAL_COMMANDS.md)

---

### 1️⃣ `./check_today.sh` - Подивитись графіки

Показує графіки відключень на **сьогодні** і **завтра** (якщо є).

```bash
./check_today.sh
```

**Приклад виводу:**
```
📅 Графік на 18.01.2026 (Today)

Основна квартира (група 4.2):
  ❌ Відключення: 00:00-01:00, 05:30-11:30, 22:00-24:00

Стара квартира (група 6.2):
  ❌ Відключення: 00:00-01:00, 04:30-08:00, 11:30-14:30, 18:30-22:00
```

---

### 2️⃣ `./plan.sh` - Скласти план на день

Аналізує графік і складає **детальний план** - коли і де працювати.

```bash
./plan.sh
```

**Приклад виводу:**
```
📅 План на 18.01.2026

⏰ 15:00-19:00: ✅ Світло скрізь - залишайся вдома
⏰ 19:00-22:00: 🏠 Тільки вдома є світло
⏰ 22:00-24:00: 🏘 Тільки на другій квартирі є світло - їдь туди!

💡 Підсумок:
🏠 Вдома: 7.0h світла
🏘 Друга квартира: 5.5h світла

✅ Рекомендація: Залишайся вдома
```

---

### 3️⃣ `./changes.sh` - Перевірити зміни

Порівнює **поточний** графік з **попереднім** і показує що змінилось.

```bash
./changes.sh
```

**Якщо змін немає:**
```
✅ Змін немає
```

**Якщо є зміни:**
```
🔄 Зміни в графіку на 18.01.2026 (Today):

  • Група 4.2: відключення скасовано ✅
  • Група 6.2: більше відключень
```

---

### 4️⃣ `./recommend.sh` - Рекомендація зараз

Швидка рекомендація - де краще бути **прямо зараз**.

```bash
./recommend.sh
```

**Приклад виводу:**
```
💡 Рекомендація (зараз 15:07):

📍 Основна квартира
💬 В обох локаціях є світло
```

---

## 🎯 Типові сценарії:

### 🌅 Ранок (найпростіше):
```bash
./status.sh        # Вся інформація відразу!
```

### 🔄 Протягом дня (швидка перевірка):
```bash
./status.sh        # Перевір зміни + план
```

### ⚡ Якщо поспішаєш:
```bash
./recommend.sh     # Тільки рекомендація
```

### 📊 Якщо потрібні деталі:
```bash
./plan.sh          # Детальний план
```

---

## 📊 Як система працює:

1. **Перший запуск `./changes.sh`** - зберігає поточний графік
2. **Наступні запуски** - порівнює з збереженим
3. **Якщо є зміни** - показує що саме змінилось
4. **Автоматично оновлює** збережений графік

**Історія зберігається в:** `.schedule_history.json`

---

## 💡 Поради:

⭐ **Найкраще:** Просто запускай `./status.sh` коли потрібно  
✅ Щоранку - `./status.sh` для планування дня  
✅ Протягом дня - `./status.sh` для перевірки змін  
✅ Перед виходом - `./recommend.sh` для швидкої рекомендації  
✅ Система автоматично розрізняє сьогодні/завтра  

---

## 🔧 Налаштування:

Твої черги вже налаштовані в `config.yaml`:
- 🏠 Основна: **4.2**
- 🏘 Стара: **6.2**

Щоб змінити - відредагуй `config.yaml`
